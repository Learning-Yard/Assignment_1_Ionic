import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yaatra.app',
  appName: 'yaatra',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
